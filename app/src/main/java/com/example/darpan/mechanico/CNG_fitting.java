package com.example.darpan.mechanico;import android.content.Intent;import android.support.v7.app.AppCompatActivity;import android.os.Bundle;import android.view.View;import android.widget.Button;import com.google.firebase.auth.FirebaseAuth;import com.google.firebase.auth.FirebaseUser;import com.google.firebase.database.DatabaseReference;import com.google.firebase.database.FirebaseDatabase;public class CNG_fitting extends AppCompatActivity {    FirebaseAuth firebaseAuth;    public static FirebaseDatabase database;    public static DatabaseReference databaseReference;    FirebaseAuth.AuthStateListener firebaseAuthListener;    FirebaseUser firebaseUser;    public static services_realtime sr2;    Button atc;    @Override    protected void onCreate(Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        setContentView(R.layout.activity_cng_fitting);        firebaseAuth=FirebaseAuth.getInstance();        services_realtime sr2=new services_realtime();        database=FirebaseDatabase.getInstance();        databaseReference=database.getReference("services_realtime");        atc=findViewById(R.id.btnatc);        String cngfit;        atc.setOnClickListener(new View.OnClickListener() {            @Override            public void onClick(View v) {                firebaseUser=FirebaseAuth.getInstance().getCurrentUser();                    String requirements = "Required";                services_realtime sr2= new services_realtime(requirements,"");                databaseReference.child(firebaseUser.getUid()).child("cng").setValue(requirements);                Intent i=new Intent(CNG_fitting.this,Cart.class);                startActivity(i);            }        });    }}